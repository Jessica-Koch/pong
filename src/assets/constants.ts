export const PADDLE_HEIGHT = 150;
export const PADDLE_WIDTH = 10;

export const PADDLE_START_HEIGHT =
  Math.floor(window.innerHeight / 2) - PADDLE_HEIGHT;

export const PADDLE_LIMIT = window.innerHeight - PADDLE_HEIGHT;

export const BOARD_BOUNDS_RIGHT = window.innerWidth - 30;
export const BOARD_BOUNDS_LEFT = 30;

export const PONG_BALL_SIZE = 20;
